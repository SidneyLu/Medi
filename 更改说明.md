# Medi 更改说明（相对 `be1852b` / 上一份更改说明）

分支：`feat/frontend-ui-polish`  
覆盖提交：`963abbf` → `cff762f` → `98551bb` → `604eaf0`

---

## 1. 合并同学报告 AI 能力

合并 `feat/report-ai-analysis-20260722`（`bc34120`）：

- 图片报告 OCR 后，用大模型筛选体检指标  
- 报告综合解读生成  
- 与现有 Postgres、聊天画像/多轮记忆并存（冲突文件已手工整合，未整文件覆盖）

---

## 2. 画像关键词增强（吸收同学 `3ac857f`，拣入式合并）

- 新增 `profile_keywords.py`：从画像抽取关键词  
- 扩展画像字段（家族史、近期症状、生活方式等）  
- 关键词进入聊天 RAG 与报告解读提示词  
- 画像页可编辑新字段并展示关键词  
- **未替换**本地 `application_repository` / schema / 历史删除等已有实现；关键词读时计算，无需额外迁库

---

## 3. 登录与 OCR 失败提示

- 会话 401 时清除失效 token，避免「登录页 ↔ 仪表盘」死循环转圈  
- OCR 核对页展示后端 `error_message`；仅占位 `OCR pending` 时给出明确提示  

---

## 4. 报告解读与引用展示

- 解读摘要按 `【小节标题】` 分段排版，去掉拥挤的内部标签展示  
- 聊天/主页来源改为静态展示，去掉「查看 PDF 原页」可点链接  

---

## 涉及文件（摘要）

| 区域 | 主要文件 |
|------|----------|
| 合并/报告 AI | `qwen_client.py`、`report_service.py`、`report_indicator_extractor.py` |
| 画像关键词 | `profile_keywords.py`、`profile_service.py`、`schemas.py`、`profile/page.tsx` |
| 登录/OCR 提示 | `app-shell.tsx`、`reports/[id]/ocr/page.tsx`、`requirements.txt` |
| 解读 UI | `reports/[id]/result/page.tsx`、`chat/page.tsx`、`dashboard/page.tsx`、`globals.css` |

---

## 未纳入 Git

- `backend/.env`（含密钥）  
- 本地前端备份目录、样例 PDF、`activate-medi.bat`  

---

## 联调提示

1. 图片 OCR 依赖 `paddlepaddle` + `paddleocr`；可用 `python scripts/warmup_paddle_ocr.py` 预热  
2. 识别中勿频繁保存代码触发 uvicorn `--reload`，以免打断请求  
3. 复杂体检表请耐心等待；或改用文字版 PDF / 裁剪检验区后上传  
